[build]
  publish = "."
  command = "echo 'No build step required'"

[build.environment]
  BACKEND_URL = "https://my-evote-backend.onrender.com"

[[redirects]]
  from = "/api/*"
  to = "https://my-evote-backend.onrender.com/api/:splat"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"